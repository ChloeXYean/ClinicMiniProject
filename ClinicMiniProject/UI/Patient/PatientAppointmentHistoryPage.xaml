<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:control="clr-namespace:ClinicMiniProject.UI.Control"
    x:Class="ClinicMiniProject.UI.Patient.PatientAppointmentHistoryPage"
    BackgroundColor="White"
    Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, Auto, *">
        
        <!-- Custom Header -->
        <control:BackNavBar Grid.Row="0" Title="Appointment History" BackCommand="{Binding BackCommand}" />
        
        <!-- Search Bar Area -->
        <Grid Grid.Row="1" ColumnDefinitions="*, Auto" Padding="20,10">
            <Frame BackgroundColor="#F0F0F0" CornerRadius="20" Padding="10" HasShadow="False" HorizontalOptions="Fill" Grid.Column="0">
                <Grid ColumnDefinitions="*, Auto">
                    <Entry Placeholder="Search Doctor Name" 
                           TextColor="Black" 
                           PlaceholderColor="Gray"
                           FontSize="14"
                           VerticalOptions="Center"/>
                    <!-- Search Icon if needed -->
                </Grid>
            </Frame>
            <ImageButton Grid.Column="1" Source="filter_icon.png" WidthRequest="24" HeightRequest="24" Margin="10,0,0,0" VerticalOptions="Center"/>
        </Grid>

        <!-- List -->
        <CollectionView Grid.Row="2" ItemsSource="{Binding Appointments}" Margin="20,0">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="15"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="{Binding CardBackgroundColor}"
                           CornerRadius="15"
                           HasShadow="True"
                           Padding="15"
                           BorderColor="Transparent">
                        <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                            
                            <!-- Row 0: Time and Date -->
                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Time}" FontAttributes="Bold" FontSize="14" TextColor="Black"/>
                            <Label Grid.Row="0" Grid.Column="1" Text="{Binding Date}" FontSize="12" TextColor="Gray"/>

                            <!-- Row 1: Details -->
                            <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Details}" FontSize="12" TextColor="Gray"/>

                            <!-- Row 2: Doctor and Status -->
                            <HorizontalStackLayout Grid.Row="2" Grid.Column="0" Spacing="5" VerticalOptions="Center">
                                <Image Source="doctor_icon_small.png" WidthRequest="16" HeightRequest="16" VerticalOptions="Center"/> 
                                <Label Text="{Binding DoctorName}" FontAttributes="Bold" FontSize="14" TextColor="Black" VerticalOptions="Center"/>
                            </HorizontalStackLayout>

                            <Frame Grid.Row="2" Grid.Column="1" 
                                   BackgroundColor="{Binding BadgeColor}" 
                                   CornerRadius="10" 
                                   Padding="10,4" 
                                   HasShadow="False"
                                   VerticalOptions="Center"
                                   HorizontalOptions="End">
                                <Label Text="{Binding Status}" FontSize="12" FontAttributes="Bold" TextColor="{Binding StatusColor}"/>
                            </Frame>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
