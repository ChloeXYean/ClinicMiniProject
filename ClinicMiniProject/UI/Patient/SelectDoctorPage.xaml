<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ClinicMiniProject.ViewModels"
             x:Class="ClinicMiniProject.UI.Patient.SelectDoctorPage"
             BackgroundColor="White"
             Shell.NavBarIsVisible="False"
             Title="Select Doctor">

    <Grid RowDefinitions="Auto, Auto, *">
        <!-- Header -->
        <Grid Grid.Row="0" BackgroundColor="#5FA8FF" Padding="15" ColumnDefinitions="*, Auto" HeightRequest="70">
            <Label Grid.Column="0"
                Text="Select Doctor"
                TextColor="White"
                FontSize="20"
                FontAttributes="Bold"
                VerticalOptions="Center"/>
            
            <!-- Exit Button -->
            <Button Grid.Column="1"
                    Text="✕"
                    BackgroundColor="Transparent"
                    TextColor="White"
                    FontSize="20"
                    FontAttributes="Bold"
                    Command="{Binding GoBackCommand}"
                    WidthRequest="40"
                    HeightRequest="40"
                    Padding="0"/>
        </Grid>

        <!-- Search Bar (Optional, can be filtering logic later) -->
        <SearchBar Grid.Row="1"
                   Placeholder="Search Doctor Name"
                   Margin="15"/>

        <!-- Doctor List -->
        <CollectionView Grid.Row="2" Margin="10" ItemsSource="{Binding AvailableDoctors}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2" HorizontalItemSpacing="10" VerticalItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame CornerRadius="15"
                           Padding="10"
                           BorderColor="#DDDDDD"
                           HasShadow="True"
                           BackgroundColor="White">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelectDoctorViewModel}}, Path=SelectDoctorCommand}" CommandParameter="{Binding .}"/>
                        </Frame.GestureRecognizers>

                        <VerticalStackLayout Spacing="5" HorizontalOptions="Center" VerticalOptions="Center">
                            <!-- Avatar Placeholder -->
                            <Frame WidthRequest="60"
                                   HeightRequest="60"
                                   CornerRadius="30"
                                   BackgroundColor="#EAEAEA"
                                   HorizontalOptions="Center"
                                   Padding="0"
                                   BorderColor="Transparent">
                                <Label Text="{Binding staff_name, StringFormat='{0:1}'}" 
                                       HorizontalOptions="Center" 
                                       VerticalOptions="Center"
                                       TextColor="Gray"
                                       FontSize="24"
                                       FontAttributes="Bold"/>
                            </Frame>

                            <Label Text="{Binding staff_name}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="Black"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"
                                   LineBreakMode="TailTruncation"/>
                                   
                            <Label Text="{Binding specialities}"
                                   FontSize="12"
                                   TextColor="Gray"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"
                                   LineBreakMode="TailTruncation"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
